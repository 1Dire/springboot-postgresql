spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/mydb  # 'localhost' 대신 'postgres'로 변경
    username: myuser
    password: mypass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  # 시간대 설정
  jackson:
    time-zone: Asia/Seoul
